name: Automated tests
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.1.1
      - uses: actions/setup-node@v3.8.1
        with:
          node-version: 18
          cache: yarn

      - name: Build
        run: |
          yarn install
          yarn build

      - name: Unit tests
        run: yarn test

      - name: Knip
        run: yarn knip
